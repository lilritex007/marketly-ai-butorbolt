# UNAS deploy – deploy-scripttag.js
# Másold: cp .env.deployment.example .env.deployment

# UNAS API (login + token)
UNAS_API_KEY=
UNAS_API_URL=https://api.unas.eu/shop

# Railway backend – API base (/api-tal végződik)
# A dist a backend-ről szolgál: SHOP_URL nélkül /api → .../dist
SHOP_URL=https://marketly-ai-butorbolt-production.up.railway.app/api

# Opcionális: CDN (ha nem Railway dist)
# Ha üres/hiányzik → cdnBase = SHOP_URL base + /dist (Railway)
# GITHUB_CDN_BASE=

# Oldal
PAGE_SLUG=butorbolt
PAGE_NAME=AI Bútorbolt
LANG=hu
